# Secrets — .env contient VITE_SUPABASE_SERVICE_ROLE_KEY et SUPABASE_DB_URL.
# Le dépôt GitHub étant public, ce fichier ne doit jamais y être versionné.
.env
.env.*
!.env.example

# Sondes de schéma jetables issues de débogages
tmp_*.mjs

# Sauvegardes de la base (npm run backup-db) : copie intégrale des données,
# mots de passe en clair compris. Ne doit jamais partir sur le dépôt.
backups/
*.dump
sauvegarde-bgm-*.json
# Enveloppe générée par `npm run schedule-backup` : chemins absolus propres au poste.
scripts/backup-auto.cmd

# Fichiers parasites de 0 octet créés par des commandes shell mal formées
({
({msg
1)
counts\[m\]
console.error('Fetch
console.error(e))

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Réglages locaux de l'agent et fichiers d'essai
.claude/settings.local.json
temp.json
